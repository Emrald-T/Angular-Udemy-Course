import { NavMenuToggleDirective } from './nav-menu-toggle.directive';

describe('NavMenuToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new NavMenuToggleDirective();
    expect(directive).toBeTruthy();
  });
});
